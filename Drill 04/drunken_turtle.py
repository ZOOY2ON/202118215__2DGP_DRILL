import turtle
import random

turtle.shape('turtle')

while (True):
          turtle.setheading(random.randint(0,50))
          turtle.forward(random.randint(50,120))
          turtle.stamp()
